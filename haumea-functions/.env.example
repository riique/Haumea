# Encryption Key (NEVER commit the real .env file!)
# Used to encrypt user API Keys in Firestore
# Must be 32 bytes (64 hexadecimal characters)
# 
# Generate with:
# - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - PowerShell: -join ((1..64) | ForEach-Object { '{0:x}' -f (Get-Random -Maximum 16) })
# - OpenSSL: openssl rand -hex 32
#
# PRODUCTION: Configure via Firebase Secret Manager
# firebase functions:secrets:set ENCRYPTION_KEY
#
ENCRYPTION_KEY=your_generated_64_char_hex_key_here

# Firebase (auto-generated by Firebase CLI)
FIREBASE_CONFIG=

# Settings
NODE_ENV=development
LOG_LEVEL=info

# OpenRouter API (optional - can be provided by user in request)
OPENROUTER_API_KEY=

# CORS Allowed Origins (comma-separated, for production)
# Example: https://yourdomain.com,https://www.yourdomain.com,https://your-project.web.app
ALLOWED_ORIGINS=